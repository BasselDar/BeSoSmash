<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('partials/head', { title: '404 - Page Not Found | BeSoSmash' }) %>
        <style>
            body {
                background: linear-gradient(-45deg, #0f172a, #1e1b4b, #312e81, #0f172a);
                background-size: 400% 400%;
            }

            .error-code {
                font-size: clamp(8rem, 20vw, 15rem);
                line-height: 1;
                background: linear-gradient(to bottom, #f43f5e, #eab308);
                -webkit-background-clip: text;
                background-clip: text;
                -webkit-text-fill-color: transparent;
                filter: drop-shadow(0 0 30px rgba(244, 63, 114, 0.3));
            }

            .glitch-wrapper {
                position: relative;
            }

            .glitch-text::before,
            .glitch-text::after {
                content: "404";
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                z-index: -1;
            }

            .glitch-text::before {
                color: #0ff;
                animation: glitch 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94) both infinite;
                clip-path: polygon(0 0, 100% 0, 100% 45%, 0 45%);
                transform: translate(-5px, -5px);
                opacity: 0.75;
            }

            .glitch-text::after {
                color: #f0f;
                animation: glitch 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94) reverse both infinite;
                clip-path: polygon(0 55%, 100% 55%, 100% 100%, 0 100%);
                transform: translate(5px, 5px);
                opacity: 0.75;
            }

            @keyframes glitch {
                0% {
                    transform: translate(0)
                }

                20% {
                    transform: translate(-2px, 2px)
                }

                40% {
                    transform: translate(-2px, -2px)
                }

                60% {
                    transform: translate(2px, 2px)
                }

                80% {
                    transform: translate(2px, -2px)
                }

                100% {
                    transform: translate(0)
                }
            }
        </style>
</head>

<body class="text-white font-sans flex flex-col items-center min-h-screen p-5 animate-gradient-x overflow-x-hidden">

    <%- include('partials/header') %>
        <%- include('partials/nav') %>

            <div class="mt-10 flex flex-col items-center text-center z-10">
                <div class="glitch-wrapper">
                    <h1 class="error-code font-black glitch-text">404</h1>
                </div>

                <h2 class="text-3xl md:text-5xl font-black mt-4 mb-6 tracking-tight">LOST IN THE VOID</h2>

                <p class="text-slate-400 text-lg md:text-xl max-w-md mx-auto mb-10 leading-relaxed font-medium">
                    Even the fastest smashers lose their way sometimes. This page doesn't exist, but high scores do.
                </p>

                <div class="flex flex-col sm:flex-row gap-4 items-center">
                    <a href="/"
                        class="px-10 py-4 bg-gradient-to-r from-rose-500 to-rose-600 rounded-2xl font-black tracking-widest text-white shadow-lg shadow-rose-500/30 hover:shadow-rose-500/50 hover:scale-105 active:scale-95 transition-all duration-300">
                        BACK TO HOMEPAGE
                    </a>
                    <a href="/classic"
                        class="px-10 py-4 bg-slate-800/80 border border-slate-700 rounded-2xl font-black tracking-widest text-slate-300 hover:text-white hover:bg-slate-700 hover:border-slate-500 transition-all duration-300">
                        GO SMASH
                    </a>
                </div>
            </div>

            <!-- Background Decoration -->
            <div class="fixed inset-0 pointer-events-none overflow-hidden opacity-20">
                <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-rose-500/30 rounded-full blur-[120px] animate-pulse">
                </div>
                <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-purple-600/30 rounded-full blur-[120px] animate-pulse"
                    style="animation-delay: 2s;"></div>
            </div>

            <%- include('partials/footer') %>

                <script>
                    // Entrance animations
                    window.addEventListener('DOMContentLoaded', () => {
                        anime({
                            targets: '.error-code',
                            translateY: [50, 0],
                            opacity: [0, 1],
                            duration: 1500,
                            easing: 'easeOutElastic(1, .8)'
                        });

                        anime({
                            targets: 'h2, p',
                            translateY: [20, 0],
                            opacity: [0, 1],
                            delay: anime.stagger(200, { start: 500 }),
                            duration: 1000,
                            easing: 'easeOutExpo'
                        });

                        anime({
                            targets: 'a',
                            scale: [0.9, 1],
                            opacity: [0, 1],
                            delay: anime.stagger(150, { start: 1000 }),
                            duration: 800,
                            easing: 'easeOutBack'
                        });
                    });
                </script>
</body>

</html>